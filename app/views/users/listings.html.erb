<div class="container">
  <% if @listings.length != 0 %>
    <h1 class= "listing-page-title">Your Listings:</h1>

    <div class="map_img-listing">
        <div
          id="map"
          style="width: 300px;
          height: 200px;
          border-radius: 5px"


          data-markers="<%= @markers.to_json %>"
          data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
    </div>
    <p class="add-spot", style="text-align:right;"><%= link_to "Add another Spot!", new_spot_path %></p>
    <% @listings.each do |listing| %>
      <br>
      <%= render "shared/listing_card", listing: listing %>
    <% end %>
  <% else %>
    <h3>You don't have any listings so far. Go ahead,</h3>
    <h1><%= link_to "Add your Spot!", new_spot_path %></h1>
  <% end %>
</div>
