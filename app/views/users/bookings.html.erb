<h1>Your Bookings:</h1>

<% @bookings.each do |booking| %>
  <h3><% booking %></h3>
  <h3>Trip to <%= booking.spot.name %>:</h3>
  <h3>From <%= booking.start_date %> to <%= booking.end_date %>  - Status: <%= booking.is_approved ? "Approved" : "Pending" %></h3>
<% end %>
